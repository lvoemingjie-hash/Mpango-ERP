{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "mpango/harness-governance/governed-paths.schema.json",
  "title": "Mpango ERP harness governance configuration",
  "description": "Governed path configuration. The validator enforces a hardcoded floor on top of this file: the minimum product prefixes (backend/, frontend/src/, scenarios/) and the governance self-protection paths can never be removed here, and empty or duplicate prefix lists are RED.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "governed_prefixes", "required_interaction_categories"],
  "properties": {
    "schema_version": { "type": "string", "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$" },
    "notes": { "type": "string" },
    "governed_prefixes": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 2,
        "pattern": "^[^*?\\[\\]]+/$",
        "description": "directory prefix, no wildcards, trailing slash required"
      }
    },
    "required_interaction_categories": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 }
    }
  }
}
