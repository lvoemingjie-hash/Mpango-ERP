# AI Context Entry

This folder is the canonical starting point for Codex and other AI coding agents working on Mpango ERP.

## Read Order

1. `docs/ai/CTO_CURRENT_OPS.md`
2. `docs/ai/CTO_COCKPIT.md`
3. `docs/ai/CTO_CONTEXT.md`
4. `docs/ai/PROJECT.md`
5. `docs/ai/PROJECT_MEMORY.md`
6. `docs/ai/AI_TEAM_OPERATING_RULES.md`
7. `docs/ai/AGENT_DELEGATION_PROTOCOL.md`
8. `docs/contracts/Boot contract.md`
9. `docs/contracts/AI workrules.md`
10. `docs/mpango_erp_v0_3_development_master_plan.md`
11. `decision-register/README.md`

## Platform Product Track Entry

For P9+ SaaS platform product work, read these after the general context above and before writing any platform product code:

1. `docs/ai/PLATFORM_PRODUCT_PRD.md`
2. `docs/ai/PLATFORM_PRODUCT_SECURITY_BOUNDARY.md`
3. `docs/ai/PLATFORM_PRODUCT_ROADMAP.md`
4. `docs/ai/PLATFORM_PRODUCT_P10_DATA_SOURCE_MAP.md`
5. `docs/ai/PLATFORM_PRODUCT_ADMIN_WORKFLOWS.md`
6. `docs/ai/PLATFORM_PRODUCT_PERMISSION_MATRIX.md`
7. `docs/ai/PLATFORM_PRODUCT_ACCEPTANCE_CRITERIA.md`
8. `docs/ai/PLATFORM_PRODUCT_CONTRACTS.md`
9. `docs/ai/PLATFORM_PRODUCT_CONTRACT_FIXTURES.md`
10. `docs/ai/PLATFORM_PRODUCT_P10A_TEST_PLAN.md`
11. `docs/ai/PLATFORM_PRODUCT_P11_FRONTEND_BOUNDARY.md`
12. `docs/ai/PLATFORM_PRODUCT_P12_SUPPORT_CONSOLE_CONTRACT.md`
13. `docs/ai/PLATFORM_PRODUCT_P13_OPERATIONS_COCKPIT_CONTRACT.md`
14. `docs/ai/PLATFORM_PRODUCT_P15_INCIDENT_TRIAGE_CONTRACT.md`
15. `docs/ai/PLATFORM_PRODUCT_P17_REGISTRY_LIFECYCLE_CONTRACT.md`
16. `docs/ai/PLATFORM_PRODUCT_P18_CONTROLLED_ACTIONS_CONTRACT.md`
17. `docs/ai/PLATFORM_PRODUCT_P19_APPROVAL_WORKFLOW_CONTRACT.md`
18. `docs/ai/PLATFORM_PRODUCT_P20_DURABLE_APPROVAL_GOVERNANCE_CONTRACT.md`
19. `docs/ai/PLATFORM_PRODUCT_P21_DURABLE_APPROVAL_STORE_CONTRACT.md`
20. `docs/ai/PLATFORM_PRODUCT_P21_DURABLE_APPROVAL_SCHEMA_PLAN.md`
21. `docs/ai/PLATFORM_PRODUCT_P21_D_DURABLE_APPROVAL_RUNTIME_ADAPTER_DESIGN_LOCK.md`
22. `docs/ai/PLATFORM_PRODUCT_P22_CONTROLLED_EXECUTION_V0_CONTRACT.md`
23. `docs/ai/PLATFORM_PRODUCT_P23_OPERATOR_TASK_NOTIFICATION_QUEUE_CONTRACT.md`
24. `docs/ai/PLATFORM_PRODUCT_P24_INCIDENT_RUNBOOK_CLOSEOUT_CONTRACT.md`
25. `docs/ai/PLATFORM_PRODUCT_P25_FRONTEND_CUSTOMER_READINESS_CONTRACT.md`

P10 must start with a data-contract-only slice unless a later CTO decision explicitly broadens scope. Do not begin P10 with migrations, API handlers, frontend UI, auth/RBAC/tenancy/session changes, payment changes, or tenant business-data edits.

P11 frontend work must start with the boundary map (`PLATFORM_PRODUCT_P11_FRONTEND_BOUNDARY.md`) and must not begin P11-B until the platform auth transport question is resolved.

P12 support console work must start with the contract (`PLATFORM_PRODUCT_P12_SUPPORT_CONSOLE_CONTRACT.md`) and must not begin P12-B (API implementation) until the contract is accepted. P12-B must not begin P12-C (frontend support UI) until API contract tests pass. No runtime code, migrations, or frontend UI in P12-A.

P13 operations observability work must start with the contract (`PLATFORM_PRODUCT_P13_OPERATIONS_COCKPIT_CONTRACT.md`) and must not begin P13-B (API implementation) until the contract is accepted. P13-B must not introduce new observability infrastructure -- it consumes existing logs, DB metadata, and runtime signals. No runtime code, migrations, or frontend UI in P13-A.

P15 incident triage work must start with the contract (`PLATFORM_PRODUCT_P15_INCIDENT_TRIAGE_CONTRACT.md`) and must not begin P15-B (read-only snapshot API adapter) until the contract is accepted. P15 is read-only triage only: no repair, no impersonation, no tenant business data, no mutation endpoints, no migrations. No runtime code, migrations, or frontend UI in P15-A.

P17 platform registry and tenant lifecycle work must start with the contract (`PLATFORM_PRODUCT_P17_REGISTRY_LIFECYCLE_CONTRACT.md`). P17 is read and contract only: no mutation of registry fields, no runtime code, no migrations, no frontend UI, and no auth/RBAC/session/tenancy/payment changes. Only future, separately approved controlled-action phases may mutate registry fields. No runtime code, migrations, or frontend UI in P17-A. P17-D-A is a docs-only backup / status source contract (`docs/ai/PLATFORM_PRODUCT_P17_BACKUP_STATUS_SOURCE_CONTRACT.md`) that defines the durable, auditable, permissionable backup / status source a future P22 `backup.check` read would need (P22-E2 returned SOURCE_UNKNOWN -- no source exists today), recommends a durable platform-schema table / model over a live ops-artifact reader, fixes the `TenantBackupStatus` mapping plus the freshness / honesty, redaction, and summary-vs-full visibility rules, and gates a future P17-D-B (schema / model / test plan) and P17-D-C (registry read wiring); it adds no runtime code, no migration, no source, and no P22 wiring -- P17-D-B and P22-E3 remain not started. P17-D-B is a docs-only / planning-only schema + model + test plan (`docs/ai/PLATFORM_PRODUCT_P17_BACKUP_STATUS_SCHEMA_PLAN.md`) that converts the accepted P17-D-A contract into implementation-ready detail (exact additive public-schema `platform_backup_outcome` / `platform_backup_policy` tables with types, nullability, defaults, indexes, uniqueness, and CHECK constraints; stored-vs-derived enum plan; the field-by-field `TenantBackupStatus` mapping with a tenant-specific-vs-platform-wide resolution rule; 24h backup freshness plus the pinned restore-test cadence; closed-vocabulary redaction; the ops-writer insert contract for `backup_postgres.sh`; the read-only registry read plan; the additive migration plan; G1-G18 tests; 16+ acceptance criteria and counterexamples), and gates a future P17-D-C (migration / ORM model / registry read wiring) and P22-E3 -- it adds no runtime code, no migration, no ORM model file, no model registration, and no P22 wiring; P17-D-C and P22-E3 remain not started.

P18 controlled platform actions work must start with the contract (`PLATFORM_PRODUCT_P18_CONTROLLED_ACTIONS_CONTRACT.md`). P18 is contract only: no execution of any controlled action, no runtime code, no migrations, no frontend UI, and no auth/RBAC/session/tenancy/payment changes. Only a separately approved future phase may implement the action-request skeleton, and even then destructive actions remain blocked unless separately approved. No runtime code, migrations, or frontend UI in P18-A.

P19 controlled action approval workflow work must start with the contract (`PLATFORM_PRODUCT_P19_APPROVAL_WORKFLOW_CONTRACT.md`). P19 is the approval boundary on top of the P18 request layer: approve and reject change approval state only, they never execute any action, and every approved approval resolves to execution_blocked with execution_allowed == false. P19 is contract only: no execution, no runtime code, no migrations, no frontend UI, no automation runner, and no auth/RBAC/session/tenancy/payment changes. Only a separately approved future phase may implement the backend approval read/write skeleton, and even then no execution, no tenant mutation, and no persistent storage unless separately gated and approved. No runtime code, migrations, or frontend UI in P19-A.

P20 durable approval governance work must start with the contract (`docs/ai/PLATFORM_PRODUCT_P20_DURABLE_APPROVAL_GOVERNANCE_CONTRACT.md`). P20 is the durable governance layer on top of P19: a persistent approval store contract (digest-only idempotency, full reason/comment redaction, retention/purge/export, unknown/degraded/read-only fallback), a dual-control policy contract (maker-checker separation, quorum, tenant-contextual identity denied, emergency override forbidden by default), and an execution readiness gate contract. Approval is not execution and durability is not execution: an approved durable approval stays at approved_execution_blocked with execution_allowed == false, every destructive or tenant-mutating action stops, and backup.restore_test_request is request-only. Cumulative state: P20-A is the docs-only contract; P20-B is a NON-EXECUTING, IN-MEMORY backend read/write skeleton (maker-checker + quorum; no migration; no database); P20-C is a READ-ONLY frontend console on top of the P20-B read path (maker-checker operator visualization, no execute control, identity-only super_admin); P20-D is the master closeout. Across all P20 slices there is no execution, no migration, no real durable backend, no tenant mutation, no notification implementation, and no auth/RBAC/session/tenancy/payment changes. Only a separately approved future phase may implement a real durable backend or a migration, and even then no execution, no tenant mutation, and no real durable backend unless separately gated and approved by the CTO. No runtime code, migrations, or frontend UI in P20-A; P20-B adds non-executing backend skeleton only; P20-C adds read-only frontend console only.

P21 durable approval store work must start with the contract (`docs/ai/PLATFORM_PRODUCT_P21_DURABLE_APPROVAL_STORE_CONTRACT.md`). P21 is the durable *storage* layer on top of P20: P20's store is in-memory / existing-safe (P20-B: no migration, no database), and P21 defines the persisted tables / logical storage records that make P20 records restart-safe (`durable_approval_requests`, `durable_approval_decisions`, `durable_approval_audit_events`, `durable_approval_idempotency_keys`, `durable_approval_retention_jobs`), plus restart-safe consistency, digest-only idempotency, redaction-before-persistence, durable append-only audit, retention / purge / export, the migration boundary, and API compatibility. Approval is not execution and durability is not execution: a durable, restart-safe approval stays at `approved_execution_blocked` with `execution_allowed == false`, every destructive or tenant-mutating action still stops, and `backup.restore_test_request` is still request-only. Cumulative state: P21-A is the docs-only contract; P21-B is a schema / migration PLAN and tests only (no production migration, no storage switch); P21-C is the EARLIEST phase that may implement the migration, and only after explicit CTO approval. Across all P21 slices there is no execution, no production migration in A/B, no tenant mutation, no AI agent execution, no notification implementation, and no auth/RBAC/session/tenancy/payment changes. No runtime code, migrations, or frontend UI in P21-A; P21-B adds plan / tests only.

P21-B durable approval store schema + test plan work must start from the accepted P21-A contract (`docs/ai/PLATFORM_PRODUCT_P21_DURABLE_APPROVAL_SCHEMA_PLAN.md`). P21-B is PLANNING-ONLY: it converts the P21-A contract into an implementation-ready schema / migration plan (exact tables, columns, types, nullability, defaults, indexes, uniqueness, and FK-like relationships for durable_approval_requests / _decisions / _audit_events / _idempotency_keys / _retention_jobs), an enum plan, a constraint plan, a transaction plan (race handling via store_version optimistic locking and SELECT FOR UPDATE), a storage-adapter interface contract (planning-only pseudocode; no backend files), a P21-C migration plan, a test plan (G1-G14), 18+ acceptance criteria, 18+ counterexamples, the P21-C entry gate, and the future AI Operator Copilot trace. P21-B creates NO migration files, NO tables, NO runtime code, NO storage switch (P20 stays in-memory / existing-safe), NO frontend, and NO tests. Approval is not execution and durability is not execution: execution_allowed stays false, executed stays false, and approved is approved_execution_blocked only. Cumulative state: P21-A (contract) is merged into platform-dev; P21-B is the docs-only plan; P21-C is the EARLIEST phase that may implement the migration, and only after explicit CTO approval (additive, platform-schema-only, no tenant schema, no storage switch); P21-D is the earliest possible runtime adapter wiring phase. Across all P21 slices there is no execution, no migration in B, no tenant mutation, no AI agent execution, no notification implementation, and no auth/RBAC/session/tenancy/payment changes. No runtime code, migrations, frontend, or backend in P21-B.

P21-D durable approval runtime adapter work must start with the discovery + design lock (`docs/ai/PLATFORM_PRODUCT_P21_D_DURABLE_APPROVAL_RUNTIME_ADAPTER_DESIGN_LOCK.md`). P21-D is the runtime adapter layer on top of P21-A/B/C: P21-C1 merged the additive, public-schema-only durable tables, but the running P20-B store is still in-memory / existing-safe, so a process restart loses every pending review, partial quorum, in-flight decision, and audit event. P21-D-a is a DOCS-ONLY design lock that records the exact P20-B runtime surface the adapter rewires (the three in-memory globals, the four service functions and their sync/async split, the four endpoints, the response models, the P18 dependency boundary) and freezes the adapter design (operation mapping, store_version optimistic locking for the quorum race, new-column population, API compatibility, unknown/degraded fallback, P18 boundary) before any P21-D runtime slice may begin. Approval is not execution and durability is not execution: the adapter preserves `execution_allowed == false`, `executed == false`, and `execution_gate == blocked`, executes nothing, mutates no P17/tenant data, and adds no new migration (it reads and writes the merged P21-C1 tables only). Cumulative state: P21-D-a is the docs-only design lock; P21-D-1 (ORM models + adapter implementation) and P21-D-2 (runtime storage cutover) are RUNTIME slices that may begin ONLY after explicit CTO approval, and even then no execution and no tenant mutation; P21-D-future (retention jobs, export, supersession, AI copilot) is each separately approved. Across all P21-D slices there is no execution, no new migration, no tenant mutation, no AI agent execution, no notification implementation, and no auth/RBAC/session/tenancy/payment changes. No runtime code, migrations, ORM model registration, frontend, or backend in P21-D-a.

P22 controlled execution work must start with the contract (`docs/ai/PLATFORM_PRODUCT_P22_CONTROLLED_EXECUTION_V0_CONTRACT.md`). P22 is the controlled *execution* layer on top of P18 through P21: P18 created action requests but did not execute, P19 approved but every approval resolved to `execution_blocked`, P20 added maker-checker / quorum and capped approvals at `approved_execution_blocked`, and P21 made approvals durable. P22 defines the bounded, dry-run-first, allowlisted, fully-audited, reversible-where-possible execution contract that may eventually run the smallest conservative action set. The v0 allowlist is exactly seven actions (`support_mode.on`, `support_mode.off`, `incident.flag_set`, `incident.flag_clear`, `provisioning.recheck`, `backup.check`, `backup.restore_test_request`); `tenant.pause`, `tenant.resume`, `lifecycle.transition`, real restore, schema migration, data deletion, payment / billing, tenant business records, and arbitrary shell / SQL / script execution are excluded forever in v0. Approval is not execution and durability is not execution: a durable, quorum-met approval at `approved_execution_blocked` is a precondition for v0 execution, never an execution itself, and even an executed v0 action never mutates tenant business data. Cumulative state: P22-A is the docs-only contract; P22-B may add a NON-EXECUTING execution skeleton (catalog read, dry-run validator, execution-request recording, execution-result read; no dispatch, no worker, no queue drain, no harness invocation) only after P22-A is accepted; real execution of any v0 action is reserved for a separately approved phase and must run through the runtime governed action adapter seam (P22-E0 supersedes the P22-D G5 gate -- the P16 worktree execution harness is a development-time / agent-worker file-change audit harness, not a runtime controlled-action executor). Across all P22 slices there is no execution in A, no migration in A, no tenant mutation, no AI agent execution, no notification implementation, and no auth/RBAC/session/tenancy/payment changes. No runtime code, migrations, or frontend UI in P22-A; P22-B adds a non-executing skeleton only; P22-C adds a NON-EXECUTING frontend operator console on top of the P22-B read/recording paths (catalog view, no-mutation dry-run, record-request only after a passed dry-run and an explicit acknowledgement, list/read; no execute control, identity-only super_admin) -- still no execution, no worker, no harness invocation, no migration, and no tenant/payment/product/auth/RBAC/session change. P22-D is the docs-only readiness closeout + real-execution design lock (no execution, no runtime/migration/test/dependency change; it closes out the as-built P22-A/B/C surface and freezes the ONLY allowed real-execution entry path -- durable approval at approved_execution_blocked, a passed P22 dry-run, explicit execution ack, an allowlisted v0 action, the runtime governed action adapter seam only (P22-E0 revised G5 -- the P16 worktree harness is not a runtime executor), a per-action adapter with no generic shell/SQL/script, and before/after/failure audit -- plus the per-action allowlist matrix, the permanent hard stops, and the P22-E entry gate, which must start from a READ-FIRST action such as backup.check or provisioning.recheck; P22-E is not started). P22-E0 is a docs-only architecture revision contract (`docs/ai/PLATFORM_PRODUCT_P22_RUNTIME_GOVERNED_ADAPTER_CONTRACT.md`) that corrects the P22-D G5 gate: the P16 worktree execution harness is a development-time / agent-worker file-change audit tool, not a runtime controlled-action executor, so future real execution must run through a runtime governed action adapter seam (per-action only; no generic shell / SQL / script / subprocess; typed request / response; preflight gate; before / after / failure audit; idempotency guard; source-status honesty; no tenant business mutation; fail closed) -- P22-E0 performs no execution and no runtime / migration / test / dependency change, changes no P16 code, and P22-E1 (a backend-only NON-EXECUTING runtime seam skeleton) may begin only after CTO acceptance of E0, with still no real backup.check until a real backup / status source (or an explicit degraded / unknown contract) is proven; approval is not execution and the seam is the only runtime execution boundary.

P23 operator task / notification queue work must start with the contract (`docs/ai/PLATFORM_PRODUCT_P23_OPERATOR_TASK_NOTIFICATION_QUEUE_CONTRACT.md`). P23 is the *aggregation and presentation* layer on top of P17 through P22: P18 created action requests, P19 / P20 / P21 gathered and durably stored approval, and P22 defined and (for `backup.check`) performed governed execution, but each left its piece of an incident scattered across its own page and ledger; P23 turns the events those phases already emit into a single, deduplicated, severity-ranked, tenant-scoped queue of operator tasks (a closed catalog of ten task types: `action_request_created`, `approval_pending`, `approval_decision_required`, `execution_ready`, `execution_completed`, `execution_failed`, `source_unknown`, `backup_check_warning`, `incident_followup_required`, `runbook_step_required`), each with a typed follow-up, a triage state machine (`open`, `acknowledged`, `in_progress`, `waiting_on_approval`, `waiting_on_source`, `completed`, `dismissed`, `expired`, `failed`), and evidence links back to the P18 / P19 / P20 / P21 / P22 / P17 ids, plus a notification *event* boundary (a recorded "attention required" event with a closed channel enum `in_app` / `email` / `webhook` that future channels may consume). A task is a view, not an executor, and a notification is a record, not a delivery: no task state transition (acknowledge, assign, in_progress, complete, dismiss) executes a P22 action, approves a P19 / P20 / P21 approval, bypasses a checker, or mutates a registry field, no notification event sends any external message, `dismissed` / `expired` never delete the underlying audit history, `source_unknown` is never displayed healthy, a `backup_check_warning` is never displayed as success, a tenant-A task never leaks into tenant-B context, and a product business task (order / payment / invoice / customer / inventory / ledger) does not belong to platform P23. Cumulative state: P23-A is the docs-only contract (task catalog, exclusion list, state machine, data-model plan, notification boundary, queue API plan, frontend plan, RBAC visibility, audit, severity / dedup / correlation, 21 acceptance criteria, 24 counterexamples, and the P23-B entry gate); P23-B may add a NON-EXECUTING, NON-SENDING backend skeleton (in-memory read model that materializes tasks from prior-phase events, task-state management endpoints as pure state transitions, dedup / severity / correlation logic, tests; no execution, no approval, no delivery, no worker, no scheduler, no frontend) only after P23-A is accepted; a real persisted queue / worker, real notification delivery, and the Operator Task Queue frontend are each reserved for separately approved phases and must stay behind the P10 identity-only platform-operator guard, the never-leaked list (no secrets / DSNs / host:port / tokens / cookies / auth headers / raw payloads / shell / SQL / tenant payload), and no auth/RBAC/session/tenancy rewrite. Across all P23 slices there is no execution, no approval bypass, no real notification delivery, no tenant business mutation, no AI agent execution or auto-approval, no auth/RBAC/session/tenancy/payment change, and no product-business-task path. No runtime code, migrations, frontend, or notification sender in P23-A.

P24 incident + runbook closeout work must start with the contract (`docs/ai/PLATFORM_PRODUCT_P24_INCIDENT_RUNBOOK_CLOSEOUT_CONTRACT.md`). P24 is the *closeout and materialization* layer on top of P15 through P23: P15 gave read-only incident triage (detect / classify / inspect / explain / handoff / close-as-observation), P18 created the `incident.flag_set` / `incident.flag_clear` action requests, P22 governs their execution (and may set / clear the P17 `incident_active` flag), and P23 aggregated all of it into the operator task queue -- but two of the ten P23 task types (`incident_followup_required`, `runbook_step_required`) were deliberately left as PUSH intake in P23-C, and the connective tissue that closes an incident out was missing. P24 defines a non-executing incident closeout lifecycle (`detected`, `triaged`, `flagged_active`, `in_remediation`, `awaiting_closeout`, `closed`, `withdrawn`, `expired`) that mirrors the P17 `incident_active` flag (never owns it), a runbook step model with three pointer kinds (`observation`, `action_pointer`, `approval_pointer`), the PUSH source boundary, and the materialization rules for those two task types (which flow through the P23 triage machine unchanged). An incident closeout is a view, not an executor, a runbook step is a pointer, not an execution, and a follow-up task is a record, not a repair: no closeout transition or step change executes a P22 action, approves an approval, sets or clears the `incident_active` flag, or mutates a registry field, `closed` requires owed-task completion and (if the flag was set) an observed flag clear through P22 `incident.flag_clear`, `withdrawn` / `expired` never delete the underlying audit history, `source_unknown` is never displayed or closed healthy, `backup_check_warning` is never displayed or closed as success, approvals are not execution (an `action_pointer` step is done only when the linked execution is observed executed, not on approval alone), a tenant-A closeout never leaks into tenant-B context, and a product business incident (order / payment / invoice / customer / inventory / ledger) does not belong to platform P24. Cumulative state: P24-A is the docs-only contract (closeout lifecycle, runbook step model, source boundaries, materialization rules, data-model plan, audit / event model, API plan, RBAC visibility, 24 acceptance criteria, 26 counterexamples, and the P24-B entry gate); P24-B may add a NON-EXECUTING, NON-SENDING backend skeleton (in-memory closeout / step read model, PUSH intake receiver, lifecycle / step state transitions as pure state changes, materialization of the two P23 task types via the existing P23-B upsert seam, dedup / severity / correlation, tests; no execution, no flag mutation, no approval, no delivery, no worker, no scheduler, no frontend) only after P24-A is accepted; a real persisted closeout / runbook store, a real intake pipeline, real notification delivery, and the Incident Closeout / Runbook frontend are each reserved for separately approved phases and must stay behind the P10 identity-only platform-operator guard, the P23 never-leaked list (no secrets / DSNs / host:port / tokens / cookies / auth headers / raw payloads / shell / SQL / tenant payload), and no auth/RBAC/session/tenancy rewrite. Across all P24 slices there is no execution, no flag mutation, no approval bypass, no real notification delivery, no tenant business mutation, no AI agent execution / auto-approval / auto-close, no auth/RBAC/session/tenancy/payment change, and no product-business-incident path. No runtime code, migrations, frontend, intake receiver, or notification sender in P24-A.
P25 platform frontend customer readiness work must start with the contract (`docs/ai/PLATFORM_PRODUCT_P25_FRONTEND_CUSTOMER_READINESS_CONTRACT.md`). P25 is NOT a new platform capability; it is a customer / operator readiness VALIDATION layer over the as-built P10-through-P24 surface (platform overview, system / tenant health, audit feed, tenant directory, support console, P13 / P14 operations observability cockpit, P15 read-only incident triage, P17 registry, P18 controlled actions, P19 approvals, P20 / P21 durable approvals, P22 controlled execution with the read-only `backup.check` governed action, P23 operator task / notification queue, and P24 incident closeout / runbook -- all behind the P10 / P11 `PlatformRoute` identity-only global `super_admin` guard that admits only a `tenant_id == null` super_admin and denies a tenant-contextual super_admin). P25 defines a closed route inventory grounded in the as-built `router/AppRouter.tsx` and `components/layout/Sidebar.tsx`, a validation matrix per route (smoke, real login where a runnable stack exists, Playwright screenshots where feasible, empty / loading / error / denied states, sidebar-to-detail-to-form navigation, copy review against the never-leaked list, no-overlap / no-clipped-text, and frontend console consistency across the P22 / P23 / P24 consoles), a safety boundary, and an evidence plan (route list, matrix result, scrubbed screenshots, commands, test counts, known warnings, demo script, defect list). Readiness is validation, not a feature: P25 adds no new backend capability, no migration, no product branch merge, no product business mutation, no auth / RBAC rewrite, no P22 execution expansion, and no notification delivery; `source_unknown` is never shown healthy, `backup_check_warning` is never shown as success, a tenant-A record never leaks into tenant-B context, and any readiness defect found (a clipped control, an overlap, a missing empty state, a tone inconsistency, a display-invariant regression) is RECORDED for a later, separately approved fix slice, not fixed inline by P25-B. Cumulative state: P25-A is the docs-only contract (readiness definition, route inventory, validation matrix, safety boundaries, evidence plan, 25 acceptance criteria, 22 counterexamples, and the P25-B entry gate); P25-B may add a NON-SHIPPING, NON-MERGING frontend validation / smoke harness only (Playwright or component-level guard / page tests with a mocked backend where no runnable stack exists; scrubbed screenshots; matrix runner; demo script; no feature implementation, no merge, no push to platform-dev) only after P25-A is accepted; any feature implementation, any real defect fix, any auth / RBAC change, any migration, any execution expansion, and any notification delivery are each reserved for separately approved phases and must stay behind the P10 identity-only platform-operator guard, the P23 never-leaked list (no secrets / DSNs / host:port / tokens / cookies / auth headers / raw payloads / shell / SQL / tenant payload), and no auth/RBAC/session/tenancy rewrite. Across all P25 slices there is no new capability, no migration, no product merge, no product business mutation, no execution expansion, no notification delivery, no tenant leak accepted as ready, no `source_unknown` shown healthy, no `backup_check_warning` shown as success, no auth/RBAC/session/tenancy/payment change, and no product-business path. No runtime code, harness, screenshots, migrations, or frontend feature in P25-A; P25-B adds a non-shipping validation / smoke harness only.

## Purpose

- Keep long-lived project context inside the repository
- Reduce dependence on transient chat history
- Align multiple AI agents on the same roadmap and constraints
- Give Codex a repeatable CTO operating surface for planning and delegation
- Give agents a short current operating picture before they enter detailed ledgers

## Update Rules

- Strategic or cross-module decisions must also be recorded in `decision-register/`
- Each meaningful AI implementation session should leave a ledger entry in `ai-ledger/`
- After each active sprint, baseline, validation gate, or agent-role change, update `CTO_CURRENT_OPS.md`
- After each meaningful phase or branch-state change, update `PROJECT.md`
- When priorities change, update `CTO_CONTEXT.md` first
- When hidden assumptions from old chats are recovered, add them to `PROJECT_MEMORY.md`
- When team operating rules for AI agents change, update `AI_TEAM_OPERATING_RULES.md`

## Multi-Agent Sync Rules

- Before starting a new task, fetch/pull the latest branch state and reread:
  - `docs/ai/README.md`
  - `docs/ai/PROJECT.md`
  - `docs/ai/PROJECT_MEMORY.md`
- If a task changes branch ownership, accepted status, current blockers, or next action, update `PROJECT.md` in the same work cycle.
- Distinguish clearly between:
  - visibility push: push so another machine / CTO can inspect the work
  - approval push: push after CTO has approved the slice for continuation or merge
- Do not assume local chat memory is shared memory. If a fact matters to another agent, it must land in repo docs or a ledger.
