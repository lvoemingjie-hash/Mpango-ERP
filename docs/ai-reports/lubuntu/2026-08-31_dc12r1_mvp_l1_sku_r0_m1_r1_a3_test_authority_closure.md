# DC-12R1-MVP-L1-SKU-R0-M1-R1-A3 — Test-Contract and Backend-Authority Closure

**Date:** 2026-08-31 | **Branch:** `codexl/dc12r1-mvp-l1-sku-r0-m1-r1-a3-test-authority-closure-2026-08-31` @ `8cef1fffe2ca92f9c368ffd23a1ce6ece7812f1b` (base candidate, unchanged)
**Verdict: PASS_FOR_CTO_DC12R1_MVP_L1_SKU_R0_M1_R1_A3_TEST_AND_BACKEND_AUTHORITY_CLOSURE_READY_FOR_BROWSER_HARNESS**

## Verdict summary

All three non-browser merge blockers from the V2 independent report are closed
with fresh evidence: (1) all 61 STALE_TEST_CONTRACT nodes reconciled test-only
and green in both orders on fresh databases; (2) `AUTHORITY_SKU_M1_BACKEND`
now binds the complete collected manifest natively through a bounded,
digest-bound transport (the 460KB single-env-string E2BIG defect is fixed and
mutation-gated); (3) one fresh full backend authority run finished
**AUTHORITY_EXECUTED_GREEN** with zero failed / zero errors / zero xpassed,
accounting gap = 0, and manifest == child proof == JUnit node identity at
3804/3804. Zero product behavior changes; no skip/deselect/xfail; the SKU
browser harness was neither authored nor run.

## Phase 0 — Evidence-truth correction to report 37448b41

Published as
`docs/ai-reports/lubuntu/2026-08-31_dc12r1_mvp_l1_sku_r0_m1_r1_a2_v2_independent_final_CORRECTIONS.md`:
the browser harness is not the sole blocker; the V2 authority result was
TEST_RED; the full manifest was not runner-bound (E2BIG at kernel
MAX_ARG_STRLEN); out-of-band node equality is diagnostic only; the "16 files"
statement is reconciled against the 11 files actually enumerated (16 was the
count of red test-class groupings, 11 is the distinct-file count — both cover
the same 61 nodes, exact table below); CURRENT_PRODUCT_DEFECT=0 is preserved
as the V2 classification and not upgraded into a merge-readiness claim.

## Phase 1 — Exact stale-contract inventory (61/61, gap = 0)

Re-captured deterministically at the base candidate on a fresh PG16 (11 files:
48 failed + 13 errors = 61 red nodes, identical file/class distribution to the
V2 authority run). Per-node table:

| # | Node ID | Kind | Exception (class, sanitized) | Old assumption | Required 038 contract | Why test-only | Falsification |
|---|---|---|---|---|---|---|---|
| 1 | `test_dc11d_payment_replay_concurrency_integrity::test_cross_tenant_same_idempotency_key_is_isolated` | failure | `  asyncpg.exceptions.UndefinedColumnError: column order_it…` | Minimal raw order_items without identity columns | Adds `sellable_unit_id`, `identity_status` (NOT NULL DEFAULT legacy), `unit_snapshot` + identity-shape CHECK to the minimal schema | Raw test-schema DDL only; replay/concurrency/idempotency assertions untouched | Seeds stay legacy-shaped rows satisfying the identity CHECK; any replay/isolation regression still fails unchanged assertions |
| 2 | `test_dc11t4h_receivable_collection_integrity::test_credit_collections_are_isolated_across_tenants` | failure | `  asyncpg.exceptions.UndefinedColumnError: column order_it…` | Same as dc11d | Fixed once in the shared helper | No edit in this file; shares the dc11d helper fix | Collection-integrity assertions unchanged |
| 3 | `test_dc12r1_s3_s1_catalog_order_hardening.TestBindingFailClosed::test_deleted_sku_rejected_on_create` | failure | `  asyncpg.exceptions.NotNullViolationError: null value in …` | Legacy-style raw SKU seed rows valid | Seed creates the CatalogProduct row first and sets `catalog_product_id` + `package_quantity` | Seed function only; all RBAC/dual-key/malformed-identity/deny assertions untouched | Deny-path assertions (403/404, dual-key exclusion) run unchanged against real catalog-identity data; re-opened paths still fail |
| 4 | `test_dc12r1_s3_s1_catalog_order_hardening.TestBindingFailClosed::test_soft_deleted_order_404` | failure | `  asyncpg.exceptions.NotNullViolationError: null value in …` | Legacy-style raw SKU seed rows valid | Seed creates the CatalogProduct row first and sets `catalog_product_id` + `package_quantity` | Seed function only; all RBAC/dual-key/malformed-identity/deny assertions untouched | Deny-path assertions (403/404, dual-key exclusion) run unchanged against real catalog-identity data; re-opened paths still fail |
| 5 | `test_dc12r1_s3_s1_catalog_order_hardening.TestDualKeyDbScoping::test_cancel_cross_supplier_404` | failure | `  asyncpg.exceptions.NotNullViolationError: null value in …` | Legacy-style raw SKU seed rows valid | Seed creates the CatalogProduct row first and sets `catalog_product_id` + `package_quantity` | Seed function only; all RBAC/dual-key/malformed-identity/deny assertions untouched | Deny-path assertions (403/404, dual-key exclusion) run unchanged against real catalog-identity data; re-opened paths still fail |
| 6 | `test_dc12r1_s3_s1_catalog_order_hardening.TestDualKeyDbScoping::test_cross_supplier_order_404` | failure | `  asyncpg.exceptions.NotNullViolationError: null value in …` | Legacy-style raw SKU seed rows valid | Seed creates the CatalogProduct row first and sets `catalog_product_id` + `package_quantity` | Seed function only; all RBAC/dual-key/malformed-identity/deny assertions untouched | Deny-path assertions (403/404, dual-key exclusion) run unchanged against real catalog-identity data; re-opened paths still fail |
| 7 | `test_dc12r1_s3_s1_catalog_order_hardening.TestDualKeyDbScoping::test_detail_wrong_retailer_404` | failure | `  asyncpg.exceptions.NotNullViolationError: null value in …` | Legacy-style raw SKU seed rows valid | Seed creates the CatalogProduct row first and sets `catalog_product_id` + `package_quantity` | Seed function only; all RBAC/dual-key/malformed-identity/deny assertions untouched | Deny-path assertions (403/404, dual-key exclusion) run unchanged against real catalog-identity data; re-opened paths still fail |
| 8 | `test_dc12r1_s3_s1_catalog_order_hardening.TestDualKeyDbScoping::test_list_excludes_wrong_retailer` | failure | `  asyncpg.exceptions.NotNullViolationError: null value in …` | Legacy-style raw SKU seed rows valid | Seed creates the CatalogProduct row first and sets `catalog_product_id` + `package_quantity` | Seed function only; all RBAC/dual-key/malformed-identity/deny assertions untouched | Deny-path assertions (403/404, dual-key exclusion) run unchanged against real catalog-identity data; re-opened paths still fail |
| 9 | `test_dc12r1_s3_s1_catalog_order_hardening.TestHappyPathPreserved::test_order_lifecycle` | failure | `  asyncpg.exceptions.NotNullViolationError: null value in …` | Legacy-style raw SKU seed rows valid | Seed creates the CatalogProduct row first and sets `catalog_product_id` + `package_quantity` | Seed function only; all RBAC/dual-key/malformed-identity/deny assertions untouched | Deny-path assertions (403/404, dual-key exclusion) run unchanged against real catalog-identity data; re-opened paths still fail |
| 10 | `test_dc12r1_s3_s1_catalog_order_hardening.TestMalformedIdentityHttp::test_cancel_malformed_retailer_id` | failure | `  asyncpg.exceptions.NotNullViolationError: null value in …` | Legacy-style raw SKU seed rows valid | Seed creates the CatalogProduct row first and sets `catalog_product_id` + `package_quantity` | Seed function only; all RBAC/dual-key/malformed-identity/deny assertions untouched | Deny-path assertions (403/404, dual-key exclusion) run unchanged against real catalog-identity data; re-opened paths still fail |
| 11 | `test_dc12r1_s3_s1_catalog_order_hardening.TestMalformedIdentityHttp::test_cancel_malformed_wholesaler_id` | failure | `  asyncpg.exceptions.NotNullViolationError: null value in …` | Legacy-style raw SKU seed rows valid | Seed creates the CatalogProduct row first and sets `catalog_product_id` + `package_quantity` | Seed function only; all RBAC/dual-key/malformed-identity/deny assertions untouched | Deny-path assertions (403/404, dual-key exclusion) run unchanged against real catalog-identity data; re-opened paths still fail |
| 12 | `test_dc12r1_s3_s1_catalog_order_hardening.TestMalformedIdentityHttp::test_detail_malformed_retailer_id` | failure | `  asyncpg.exceptions.NotNullViolationError: null value in …` | Legacy-style raw SKU seed rows valid | Seed creates the CatalogProduct row first and sets `catalog_product_id` + `package_quantity` | Seed function only; all RBAC/dual-key/malformed-identity/deny assertions untouched | Deny-path assertions (403/404, dual-key exclusion) run unchanged against real catalog-identity data; re-opened paths still fail |
| 13 | `test_dc12r1_s3_s1_catalog_order_hardening.TestMalformedIdentityHttp::test_detail_malformed_wholesaler_id` | failure | `  asyncpg.exceptions.NotNullViolationError: null value in …` | Legacy-style raw SKU seed rows valid | Seed creates the CatalogProduct row first and sets `catalog_product_id` + `package_quantity` | Seed function only; all RBAC/dual-key/malformed-identity/deny assertions untouched | Deny-path assertions (403/404, dual-key exclusion) run unchanged against real catalog-identity data; re-opened paths still fail |
| 14 | `test_dc12r1_s3_s1_catalog_order_hardening.TestSameSchemaWrongEntityExclusion::test_list_excludes_wrong_wholesaler` | failure | `  asyncpg.exceptions.NotNullViolationError: null value in …` | Legacy-style raw SKU seed rows valid | Seed creates the CatalogProduct row first and sets `catalog_product_id` + `package_quantity` | Seed function only; all RBAC/dual-key/malformed-identity/deny assertions untouched | Deny-path assertions (403/404, dual-key exclusion) run unchanged against real catalog-identity data; re-opened paths still fail |
| 15 | `test_dc12r1_s3_s2b_i1_financial_schema_foundation.TestMigrationHead::test_head_is_037` | failure | `  AssertionError: assert '038_catalog_...ertical_slice' ==…` | Sole head == 037 | Sole head == 038 (node renamed `test_head_is_038`) | Assertion constant + node name only; sole-head count assertion untouched | Exact-equality assert still fails on any head regression or extra head |
| 16 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestExactCatalogShapeBypass::test_amount_range_constraint_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 17 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestExactCatalogShapeBypass::test_check_extra_and_condition_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 18 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestExactCatalogShapeBypass::test_composite_fk_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 19 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestExactCatalogShapeBypass::test_composite_pk_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 20 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestExactCatalogShapeBypass::test_computed_status_default_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 21 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestExactCatalogShapeBypass::test_index_expression_key_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 22 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestExactCatalogShapeBypass::test_next_seq_computed_default_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 23 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestExactCatalogShapeBypass::test_prefix_compatible_wrong_type_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 24 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestRealAlembicUpgradeFailClosed::test_amount_ge_zero_weakening_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 25 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestRealAlembicUpgradeFailClosed::test_amount_or_1_equals_1_weakening_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 26 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestRealAlembicUpgradeFailClosed::test_canonical_upgrade_reaches_037_and_second_run_noops` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 27 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestRealAlembicUpgradeFailClosed::test_check_or_true_weakening_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 28 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestRealAlembicUpgradeFailClosed::test_check_wrong_column_identity_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 29 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestRealAlembicUpgradeFailClosed::test_fk_cascade_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 30 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestRealAlembicUpgradeFailClosed::test_next_seq_wrong_default_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 31 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestRealAlembicUpgradeFailClosed::test_permission_collision_fails_upgrade` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 32 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestRealAlembicUpgradeFailClosed::test_receipt_index_weakened_predicate_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 33 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestRealAlembicUpgradeFailClosed::test_receipt_index_wrong_key_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 34 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestRealAlembicUpgradeFailClosed::test_receipt_sequences_extra_column_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 35 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestRealAlembicUpgradeFailClosed::test_regular_index_extra_predicate_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 36 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestRealAlembicUpgradeFailClosed::test_regular_index_wrong_uniqueness_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 37 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestRealAlembicUpgradeFailClosed::test_status_default_trailing_space_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 38 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestRealAlembicUpgradeFailClosed::test_status_missing_default_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 39 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestRealAlembicUpgradeFailClosed::test_status_not_equal_chain_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 40 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestRealAlembicUpgradeFailClosed::test_status_or_1_equals_1_weakening_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 41 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestRealAlembicUpgradeFailClosed::test_unbounded_transaction_id_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 42 | `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.TestRealAlembicUpgradeFailClosed::test_wrong_index_keys_rejected` | failure | `  038_catalog_identity_vertical_slice_py.PreflightFailure:…` | Fixture reverts only 037 additions; `upgrade head` == 037; final head pinned 037 | Fixture also reverts 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head pinned 038; one node renamed `…_reaches_head_038_…` | Fixture/seed + head-assertion edits only; malformed-migration RED legs, `_assert_preflight_failure`, and fingerprint-unchanged assertions untouched | Every repaired test still executes its real malformed-migration leg asserting PreflightFailure + unchanged fingerprint; removing the 038 preflight is still caught by `test_sku_m1_migration_pg16.py` preflight tests |
| 43 | `test_dc12r1_s3_s2b_i2a_canonical_payment_service::test_service_cross_tenant_same_key_isolated` | failure | `  asyncpg.exceptions.UndefinedColumnError: column order_it…` | Same as dc11d | Fixed once in the shared helper | No edit in this file; shares the dc11d helper fix | Cross-tenant idempotency-key isolation assertions unchanged |
| 44 | `test_phase3_pricing::test_get_price_returns_correct_price` | error | `  asyncpg.exceptions.NotNullViolationError: null value in …` | SKU rows may exist without an owning CatalogProduct | Seed inserts one CatalogProduct per SKU (id == sku id — the exact 038 reconciliation shape) and sets `catalog_product_id` + `package_quantity` | Fixture seed only; every pricing assertion (price resolution, isolation, zero/negative rejection) untouched | Pricing assertions unchanged; seeds now satisfy the same NOT NULL + FK contract production enforces, so price-resolution regressions still fail unchanged assertions |
| 45 | `test_phase3_pricing::test_get_price_returns_none_for_unpriced` | error | `  asyncpg.exceptions.NotNullViolationError: null value in …` | SKU rows may exist without an owning CatalogProduct | Seed inserts one CatalogProduct per SKU (id == sku id — the exact 038 reconciliation shape) and sets `catalog_product_id` + `package_quantity` | Fixture seed only; every pricing assertion (price resolution, isolation, zero/negative rejection) untouched | Pricing assertions unchanged; seeds now satisfy the same NOT NULL + FK contract production enforces, so price-resolution regressions still fail unchanged assertions |
| 46 | `test_phase3_pricing::test_get_prices_bulk` | error | `  asyncpg.exceptions.NotNullViolationError: null value in …` | SKU rows may exist without an owning CatalogProduct | Seed inserts one CatalogProduct per SKU (id == sku id — the exact 038 reconciliation shape) and sets `catalog_product_id` + `package_quantity` | Fixture seed only; every pricing assertion (price resolution, isolation, zero/negative rejection) untouched | Pricing assertions unchanged; seeds now satisfy the same NOT NULL + FK contract production enforces, so price-resolution regressions still fail unchanged assertions |
| 47 | `test_phase3_pricing::test_negative_price_rejected_at_db_level` | error | `  asyncpg.exceptions.NotNullViolationError: null value in …` | SKU rows may exist without an owning CatalogProduct | Seed inserts one CatalogProduct per SKU (id == sku id — the exact 038 reconciliation shape) and sets `catalog_product_id` + `package_quantity` | Fixture seed only; every pricing assertion (price resolution, isolation, zero/negative rejection) untouched | Pricing assertions unchanged; seeds now satisfy the same NOT NULL + FK contract production enforces, so price-resolution regressions still fail unchanged assertions |
| 48 | `test_phase3_pricing::test_order_total_from_resolved_prices` | error | `  asyncpg.exceptions.NotNullViolationError: null value in …` | SKU rows may exist without an owning CatalogProduct | Seed inserts one CatalogProduct per SKU (id == sku id — the exact 038 reconciliation shape) and sets `catalog_product_id` + `package_quantity` | Fixture seed only; every pricing assertion (price resolution, isolation, zero/negative rejection) untouched | Pricing assertions unchanged; seeds now satisfy the same NOT NULL + FK contract production enforces, so price-resolution regressions still fail unchanged assertions |
| 49 | `test_phase3_pricing::test_product_query_price_isolation_between_retailers` | error | `  asyncpg.exceptions.NotNullViolationError: null value in …` | SKU rows may exist without an owning CatalogProduct | Seed inserts one CatalogProduct per SKU (id == sku id — the exact 038 reconciliation shape) and sets `catalog_product_id` + `package_quantity` | Fixture seed only; every pricing assertion (price resolution, isolation, zero/negative rejection) untouched | Pricing assertions unchanged; seeds now satisfy the same NOT NULL + FK contract production enforces, so price-resolution regressions still fail unchanged assertions |
| 50 | `test_phase3_pricing::test_product_query_with_price_join` | error | `  asyncpg.exceptions.NotNullViolationError: null value in …` | SKU rows may exist without an owning CatalogProduct | Seed inserts one CatalogProduct per SKU (id == sku id — the exact 038 reconciliation shape) and sets `catalog_product_id` + `package_quantity` | Fixture seed only; every pricing assertion (price resolution, isolation, zero/negative rejection) untouched | Pricing assertions unchanged; seeds now satisfy the same NOT NULL + FK contract production enforces, so price-resolution regressions still fail unchanged assertions |
| 51 | `test_phase3_pricing::test_retailer_b_has_no_sugar_price` | error | `  asyncpg.exceptions.NotNullViolationError: null value in …` | SKU rows may exist without an owning CatalogProduct | Seed inserts one CatalogProduct per SKU (id == sku id — the exact 038 reconciliation shape) and sets `catalog_product_id` + `package_quantity` | Fixture seed only; every pricing assertion (price resolution, isolation, zero/negative rejection) untouched | Pricing assertions unchanged; seeds now satisfy the same NOT NULL + FK contract production enforces, so price-resolution regressions still fail unchanged assertions |
| 52 | `test_phase3_pricing::test_retailer_price_isolation` | error | `  asyncpg.exceptions.NotNullViolationError: null value in …` | SKU rows may exist without an owning CatalogProduct | Seed inserts one CatalogProduct per SKU (id == sku id — the exact 038 reconciliation shape) and sets `catalog_product_id` + `package_quantity` | Fixture seed only; every pricing assertion (price resolution, isolation, zero/negative rejection) untouched | Pricing assertions unchanged; seeds now satisfy the same NOT NULL + FK contract production enforces, so price-resolution regressions still fail unchanged assertions |
| 53 | `test_phase3_pricing::test_set_price_creates_new` | error | `  asyncpg.exceptions.NotNullViolationError: null value in …` | SKU rows may exist without an owning CatalogProduct | Seed inserts one CatalogProduct per SKU (id == sku id — the exact 038 reconciliation shape) and sets `catalog_product_id` + `package_quantity` | Fixture seed only; every pricing assertion (price resolution, isolation, zero/negative rejection) untouched | Pricing assertions unchanged; seeds now satisfy the same NOT NULL + FK contract production enforces, so price-resolution regressions still fail unchanged assertions |
| 54 | `test_phase3_pricing::test_set_price_updates_existing` | error | `  asyncpg.exceptions.NotNullViolationError: null value in …` | SKU rows may exist without an owning CatalogProduct | Seed inserts one CatalogProduct per SKU (id == sku id — the exact 038 reconciliation shape) and sets `catalog_product_id` + `package_quantity` | Fixture seed only; every pricing assertion (price resolution, isolation, zero/negative rejection) untouched | Pricing assertions unchanged; seeds now satisfy the same NOT NULL + FK contract production enforces, so price-resolution regressions still fail unchanged assertions |
| 55 | `test_phase3_pricing::test_unpriced_product_has_can_order_false` | error | `  asyncpg.exceptions.NotNullViolationError: null value in …` | SKU rows may exist without an owning CatalogProduct | Seed inserts one CatalogProduct per SKU (id == sku id — the exact 038 reconciliation shape) and sets `catalog_product_id` + `package_quantity` | Fixture seed only; every pricing assertion (price resolution, isolation, zero/negative rejection) untouched | Pricing assertions unchanged; seeds now satisfy the same NOT NULL + FK contract production enforces, so price-resolution regressions still fail unchanged assertions |
| 56 | `test_phase3_pricing::test_zero_price_rejected_at_order_time` | error | `  asyncpg.exceptions.NotNullViolationError: null value in …` | SKU rows may exist without an owning CatalogProduct | Seed inserts one CatalogProduct per SKU (id == sku id — the exact 038 reconciliation shape) and sets `catalog_product_id` + `package_quantity` | Fixture seed only; every pricing assertion (price resolution, isolation, zero/negative rejection) untouched | Pricing assertions unchanged; seeds now satisfy the same NOT NULL + FK contract production enforces, so price-resolution regressions still fail unchanged assertions |
| 57 | `test_s5a_fresh_tenant_real_user_journey_gate::test_s5a_fresh_tenant_real_user_journey_gate` | failure | `  asyncpg.exceptions.NotNullViolationError: null value in …` | Legacy-style raw SKU seed valid | Seed creates the CatalogProduct row first and sets `catalog_product_id` + `package_quantity` | Seed block only; journey, stock, ledger and refund assertions untouched | Cross-tenant isolation still exercised with a real catalog-identity SKU; journey failures still surface |
| 58 | `test_u3b2_preview_validate.TestNoSkuInventoryWrites::test_no_inventory_import[filepath1]` | failure | `  assert 'inventory_repository' not in '"""U3-B2/B3...retu…` | Import path may not reference the inventory repository at all (string ban) | Router: full string ban kept. Service: AST guard allows exactly one `InventoryRepository` import, instantiation only as injected/default field, method calls exactly `{ensure_stock_row}`, only inside `apply*` functions; `inventory_stock`/`stock_movement`/`retailer_price` string bans kept | Guard-only; preview/validate/apply business assertions untouched | New `test_ensure_stock_row_delegation_is_apply_phase_only` fails on any non-apply call; `test_no_inventory_import` still fails on any second method, direct class-attribute access, or router reference |
| 59 | `test_u3b2_preview_validate.TestNoSkuInventoryWrites::test_no_inventory_or_pricing_writes` | failure | `  assert 'inventory_repository' not in '"""U3-B2/B3...retu…` | Import path may not reference the inventory repository at all (string ban) | Router: full string ban kept. Service: AST guard allows exactly one `InventoryRepository` import, instantiation only as injected/default field, method calls exactly `{ensure_stock_row}`, only inside `apply*` functions; `inventory_stock`/`stock_movement`/`retailer_price` string bans kept | Guard-only; preview/validate/apply business assertions untouched | New `test_ensure_stock_row_delegation_is_apply_phase_only` fails on any non-apply call; `test_no_inventory_import` still fails on any second method, direct class-attribute access, or router reference |
| 60 | `test_u6f_onboarding_auth_chain_closeout::test_migration_schema_sanity_uses_current_single_head` | failure | `  AssertionError: assert ['038_catalog...rtical_slice'] ==…` | Sole head == 037 | Sole head == 038 (heads + current head) | Assertion constants only | Exact list equality still fails on head regression or second head |
| 61 | `test_u6i1_owner_credential_setup_schema::test_alembic_head_includes_owner_credential_setup_tokens` | failure | `  AssertionError: assert ['038_catalog...rtical_slice'] ==…` | Sole head == 037 | Sole head == 038 | Assertion constant only | Exact list equality still fails on head regression |

Total rows: 61 — accounting 61/61, gap = 0


## Phase 2 — Test-only reconciliation (allowed changes only)

| File(s) | Change | Contract |
|---|---|---|
| `test_dc12r1_s3_s2b_i1_r4_r1_real_alembic_upgrade.py` | fixture additionally reverts the 038 catalog-identity objects (genuine pre-038 tenant); `_full_proof` step 3 pins EXACT 037; final head assertions → 038; one node renamed | migration fixtures construct a real pre-038 tenant; head assertions bind the current candidate |
| `test_dc12r1_s3_s2b_i1_financial_schema_foundation.py`, `test_u6f_onboarding_auth_chain_closeout.py`, `test_u6i1_owner_credential_setup_schema.py` | head assertions 037 → exact 038 (one node renamed `test_head_is_038`) | current single head is the SKU candidate's 038 |
| `test_phase3_pricing.py`, `test_dc12r1_s3_s1_catalog_order_hardening.py`, `test_s5a_fresh_tenant_real_user_journey_gate.py` | SKU seeds create the owning CatalogProduct row (id == sku id, the 038 reconciliation shape) and set `catalog_product_id`/`package_quantity` | seeds supply valid catalog identity |
| `test_s5a_fresh_tenant_real_user_journey_gate.py` (isolation block) | order item seeded with `sellable_unit_id`/`identity_status='stable'`/`unit_snapshot` instead of a legacy-null item | raw order_items carry current identity columns |
| `test_dc11d_payment_replay_concurrency_integrity.py` (shared helper, also used by `test_dc11t4h…` and `test_dc12r1_s3_s2b_i2a…`) | minimal tenant schema's `order_items` gains `sellable_unit_id`, `identity_status` (NOT NULL DEFAULT legacy), `unit_snapshot` + identity-shape CHECK | raw schemas include current identity columns |
| `test_u3b2_preview_validate.py` | AST guards accept exactly one `InventoryRepository` import, instantiation as injected/default field, and `ensure_stock_row` calls only inside `apply*`; router string ban and all `inventory_stock`/`stock_movement`/`retailer_price` bans kept; new `test_ensure_stock_row_delegation_is_apply_phase_only` | guard updated for the sanctioned delegation without weakening |

No skips, deselects, xfails, or assertion weakenings; migration 038 untouched;
no H2-C / PRICING / ORDER-PRICE / REORDER files touched.

**Scope disclosure (test-only, requirement-driven):** two additional test files
were stabilized because frozen manifests cannot bind volatile node IDs at all —
`test_u4d_intake_parser_preview.py` (xlsx params embedded zip headers with
collection-time timestamps → per-run volatile IDs; explicit `pytest.param` ids
added) and `test_u6i3_owner_credential_setup_consume.py` (uuid4 in a param →
literal literal token). No test body or assertion changed. This is disclosed as
an A3 extension beyond the 11-file Phase-1 set; classification
TEST_INFRASTRUCTURE (test-side ID instability), not product behavior.

## Phase 3 — Runner transport closure

**Defect (pre-existing infrastructure):** `collect_proven` placed the entire
node list in `ET1_RUNNER_REQUIRED_NODES` — a single environment string
(460,335 bytes for 3803 nodes vs kernel `MAX_ARG_STRLEN` 131,072) — so the
collect child spawn died `OSError: [Errno 7] Argument list too long` before any
launch. Pre-fix reproduction captured on this branch's base
(`/tmp` evidence, exit 1, `PRE-FIX EXIT=1` in the run log).

**Fix (bounded, immutable transport):**

- runner writes ONE canonical transport file (UTF-8, sorted, unique,
  LF-terminated lines) next to the collect proof, binds its raw-byte SHA-256
  into `ET1_RUNNER_MANIFEST_TRANSPORT_DIGEST`; no argv/env string ever carries
  the manifest;
- child (`pytest_et1_collector.py`) loads the canonical bytes itself and
  independently re-derives the digest; missing file, substituted file, byte
  drift, digest mismatch, duplicate node, non-canonical order, blank lines, and
  a missing trailing newline (truncation) each fail closed with fixed
  `manifest_transport:*` labels in BOTH `pytest_sessionstart` and
  `pytest_collection_finish`;
- runner-side `verify_child_proof` cross-compares the child's re-derived
  digest and node total against the runner's ORIGINAL;
- JIT drift re-verification at collect-complete, `authorize()`, and `run()`
  (post-preflight mutation blocks the launch); runner-owned file removed on
  every exit path (`main()` finally);
- no fallback to profile default nodes: an unbound transport fails closed;
- manifest parsing switched to LF-only splitting so node IDs carrying embedded
  CR bytes (binary xlsx params) survive; the pre-existing mixed-EOL trap is
  kept;
- evidence publishes only booleans (`manifest_transport_bound/match`).

**Truth + mutation tests:** new `test_authority_runner_r4_transport.py`
(22 tests: canonical encoding, every fail-closed category, cross-process digest
compare, drift gates, source-contract regression guard) and `et1_r4_mutations.py`
(6 probes: R401 env-string restored, R402 child digest compare deleted, R403
canonical-order check deleted, R404 drift recheck deleted, R405 child transport
compare deleted, R406 post-collect drift check deleted) wired into
`run_red_mutations.py`. Prior-round seed helpers were transport-bound so their
probes target their own defects.

**Proof old fails / new passes:** pre-fix `--collect-only` with the full
manifest → exit 1, `OSError: [Errno 7] Argument list too long`; post-fix the
same command → `COLLECT: PASS count=3804`. Intermediate VOID results during
bring-up were correct fail-closed behavior (stale 3803 manifest vs 3804 collected;
byte-mangled manifest) — each was fixed by freezing the manifest at the live
tree, never by weakening a check.

**Governance:** `PD-2026-08-31-SKU-R0-M1-R1-A3` registered (kind governance,
base `24a28d76`) enumerating every changed path. Structural validator
`--baseline-ref origin/product-dev-recovered`: structural=PASS (release=BLOCKED
by 3 pre-registered debts — the expected PR-gate state).

## Phase 4 — Verification (fresh PostgreSQL 16 + Redis 7)

| # | Gate | Result |
|---|---|---|
| 1 | exact 61-node bundle, natural order, fresh DB | **61 passed / 0 failed** (224s) |
| 2 | exact 61-node bundle, reverse order, other fresh DB | **61 passed / 0 failed** (248s) |
| 3 | neighboring tests (11 reconciled files fully + 3 SKU-M1 files + pricing-safe-orders + U3C×2 + U4IB2 + six S4 files) | **401 passed / 0 failed** (496s) |
| 4a | harness-governance unit suite | **212 passed / 0 failed** |
| 4b | harness-governance red mutation gate | **108 RED as intended (incl. 6 new R4) / 9 GREEN controls / TREE INTEGRITY OK, exit 0** |
| 4c | runner `--self-test` | OK |
| 5 | full backend authority, exactly once, `AUTHORITY_SKU_M1_BACKEND`, native full-manifest binding | **AUTHORITY_EXECUTED_GREEN**, `sentinel_calls=1`, `collect_child_spawns=1`, 1942s |
| 6 | `REDIS_URL` and `PW1R3_TEST_REDIS_URL` both exported to the runner and authority command | yes (Redis DB15 empty, sentinel 26379 unreachable, verified pre-run) |
| 7 | runner binds the complete collected manifest natively | `expected_node_count=3804`, `collected_node_count=3804`, `manifest_transport_bound=true`, `manifest_transport_match=true` |
| 8 | collected manifest == child proof == JUnit node set | **identical, 3804/3804, zero diff** (module-path→file-path resolved, disk-checked) |
| 9 | final authority result | **3741 passed / 0 failed / 0 errors / 0 xpassed / 48 skipped / 15 xfailed; 3741+48+15 = 3804 = collected → gap = 0** |
| 10 | no authority rerun | exactly one launch (`sentinel_calls=1`); no red occurred, none needed |

Authority environment: fresh `postgres:16-alpine` + `redis:7-alpine` containers
(loopback ports 17720/17721), non-superuser CREATEDB+CREATEROLE role
(`rolsuper=f`), chain applied to head `038_catalog_identity_vertical_slice`,
`MPANGO_ENV=test`, temp-DB port/host allowlists set, `REPORTING_USER_PASSWORD`
set, `J1H2C_RETAILER_EMAIL` default (resolvable domain).

## Required explicit statements

```
H2-C_NOT_EVALUATED
SKU_BROWSER_HARNESS_NOT_AUTHORED_OR_RUN
PRICING_NOT_STARTED
ORDER_PRICE_NOT_STARTED
REORDER_NOT_STARTED
```

## Cleanup

Task-owned containers (`sku_a3_pg16`, `sku_a3_redis7`), network `sku_a3_net`,
databases (`test_sku_a3_stale/_bundle_a/_bundle_b/_authority`), worktree
`dc12r1-sku-a3-runtime`, venv `.venv-a3`, and `/tmp/a3_*` artifacts destroyed
after the branch push; host-owner resources untouched.

## CTO hand-off

With this closure, the remaining controlled-merge blocker for `8cef1fff` is
exactly the one the V2 verdict named: authorizing the SKU browser harness for
CATALOG-ID-001 / CATALOG-HIST-001 (desktop + 390px). This branch is ready for
that browser-harness task.

**Final verdict: PASS_FOR_CTO_DC12R1_MVP_L1_SKU_R0_M1_R1_A3_TEST_AND_BACKEND_AUTHORITY_CLOSURE_READY_FOR_BROWSER_HARNESS**
