# =============================================================================
# Mpango ERP — Production-like Docker Compose (Track E2)
#
# R1-R3 FAIL-CLOSED
# (CTO-AUTH-ORDER-R2-DB-AUTHORITY-INTEGRATION-R1-R3-TOPOLOGY-AND-READINESS-CLOSURE-2026-09-19):
# this file is INTENTIONALLY NON-RUNNABLE.  Its legacy topology — PostgreSQL
# 15, an application-schema initializer mount, a single administrator identity
# injected into the backend runtime, and the reporting-user password in the
# runtime environment — contradicts the accepted three-role authority model.
# ANY `docker compose -f docker-compose.prod.yml ...` invocation fails at the
# required-variable interpolation below (verified by a permanent test).  A
# real production deployment contract is a separately authorized task;
# until it lands, use docker-compose.yml together with the five-stage
# backend/scripts/setup.sh sequence.
# =============================================================================
services:
  r1r3-prod-compose-fail-closed:
    image: ${R1R3_PROD_COMPOSE_IS_DISABLED:?docker-compose.prod.yml is intentionally fail-closed pending the separately authorized deployment-contract task - use docker-compose.yml with the five-stage setup.sh sequence}
