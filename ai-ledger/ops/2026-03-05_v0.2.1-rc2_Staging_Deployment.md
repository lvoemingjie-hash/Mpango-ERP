# 2026-03-05 — v0.2.1-rc2 Staging Deployment

**Role:** OPS AI
**Target:** 143.110.177.2 (Staging VPS)
**Tag:** `v0.2.1-rc2` → Commit `b715518`
**Time:** 2026-03-05 14:00 UTC+8

---

## Context

RC1 deployment failed due to:
1. Missing `backend/api/v1/auth.py` in git (caused 500 on login)
2. Migration 016 enum failure

Backend AI fixed both issues and tagged `v0.2.1-rc2`. CTO approved OPS to deploy.

---

## Deployment Steps Executed

### 1. Code Sync
```bash
cd /root/mpango-erp
git fetch origin
git reset --hard origin/main
git checkout v0.2.1-rc2
```
Result: HEAD at `b715518` — "fix(auth): remove tenant_code dependency from login flow"

### 2. Rebuild Backend
```bash
docker compose -f docker-compose.prod.yml build --no-cache backend
docker compose -f docker-compose.prod.yml up -d
```
Result: Backend container rebuilt and running with new image.

### 3. Database Migration
```bash
docker compose exec backend alembic upgrade head
docker compose exec backend alembic current
```
Result: `016_add_returned_status (head)` ✅

### 4. Health Check
```bash
curl http://localhost/health/live
```
Result:
```json
{"status":"healthy","service":"mpango-erp-backend","version":"0.2.0","timestamp":"2026-03-05T06:02:25.093521"}
```

### 5. Login Endpoint Test
```bash
curl -X POST http://localhost/api/v1/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"admin@test.com","password":"testpass123"}'
```
Result: `{"code":"UNAUTHORIZED","message":{"code":"INVALID_CREDENTIALS","message":"Invalid credentials"}}`

✅ No 500 error — login endpoint works (401 for invalid credentials is expected)

---

## Service Status

| Service | Status |
|---------|--------|
| postgres | Running ✅ |
| redis | Running ✅ |
| backend | Running ✅ |
| frontend | Running ✅ |
| gateway | Running ✅ |

---

## Notes

- No seed data script available in `/app/scripts/` (no `seed_demo_data.py`). Login returns 401 because no test users exist in the DB.
- Frontend is serving at http://143.110.177.2
- Backend health endpoint working at `/health/live`

---

## Next Steps

1. Create a test tenant with admin user to verify full login flow
2. Or run `create_wholesaler.py` with test data to seed the environment

---

**Status:** SYSTEM ONLINE: RC2 DEPLOYED
